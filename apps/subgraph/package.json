{
  "name": "@octant/subgraph",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build --network localhost",
    "dev": "./scripts/dev.sh",
    "clean": "rm -rf build generated"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.93.2",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
