# @octant/validation

Zod schemas for content validation across the Octant blog.

## Import

```ts
import {
  SignupInputSchema,
  LoginInputSchema,
  RefreshTokenInputSchema,
  WeatherDataSchema,
  CreateRoundInputSchema,
  SubmitVoteInputSchema,
} from '@octant/validation';

// Generated model schemas (MongoDB)
import { /* UserModelSchema, SessionModelSchema, ... */ } from '@octant/validation';

// Generated model schemas (Postgres)
import { postgres, postgresInput } from '@octant/validation';
```

TypeScript autocomplete covers the full export surface.
